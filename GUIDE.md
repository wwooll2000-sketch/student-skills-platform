# 📚 دليل استخدام منصة مهارات الطلاب - شامل

## 📋 فهرس المحتوى
1. [البدء السريع](#البدء-السريع)
2. [ملفات النظام](#ملفات-النظام)
3. [الأمان والحماية](#الأمان-والحماية)
4. [النسخ الاحتياطية](#النسخ-الاحتياطية)
5. [الوضع بدون اتصال](#الوضع-بدون-اتصال)
6. [أدوات المعلم](#أدوات-المعلم)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🚀 البدء السريع

### للطالب:
1. افتح الموقع في المتصفح
2. أدخل رقم الطالب الذي أعطاه لك المعلم
3. اضغط "دخول"
4. تصفح مهاراتك المفقودة
5. انقر على الملفات لعرض الشروح التفصيلية

### للمعلم:
1. اضغط على رمز ⚙️ في الزاوية العلوية اليسرى
2. أدخل كلمة المرور: `admin`
3. ستنتقل إلى لوحة التحكم
4. أضف طلاب جدد
5. أضف مهارات لكل طالب
6. تابع تقدم الطلاب

---

## 📁 ملفات النظام

### الملفات الأساسية:

| الملف | الوصف | الدور |
|------|--------|------|
| `index.html` | الملف الرئيسي للموقع | الواجهة الأمامية |
| `manifest.json` | تكوين التطبيق | دعم PWA والهاتف |
| `service-worker.js` | خدمة الويب | الوضع بدون اتصال |
| `security.js` | نظام الأمان | الحماية والتشفير |
| `backup-data.js` | النسخ الاحتياطية | حفظ واستعادة البيانات |
| `enhancements.js` | الإضافات | تحسينات اضافية |

### ملفات الخادم:

| الملف | الوصف |
|------|--------|
| `.htaccess` | قوانين خادم Apache |
| `robots.txt` | توجيهات محركات البحث |
| `sitemap.xml` | خريطة الموقع |
| `.well-known/security.txt` | معلومات الأمان |
| `config.json` | إعدادات التطبيق |

---

## 🔐 الأمان والحماية

### المميزات الأمنية:

#### 1️⃣ حماية من هجمات XSS
```javascript
// تنظيف البيانات المدخلة
const cleanData = securityManager.sanitizeHTML(userInput);
```

#### 2️⃣ التحقق من صحة الإدخال
```javascript
// التحقق من صيغة البيانات
if (securityManager.validateInput(data, 'url')) {
    // البيانات صحيحة
}
```

#### 3️⃣ حماية من محاولات الدخول المتكررة
- 5 محاولات فاشلة = قفل لمدة 15 دقيقة
- تسجيل كل محاولة
- إعادة تعيين عند النجاح

#### 4️⃣ رؤوس HTTP الأمان
```
X-XSS-Protection: 1; mode=block
X-Frame-Options: SAMEORIGIN
X-Content-Type-Options: nosniff
Content-Security-Policy: [محدد]
```

#### 5️⃣ تسجيل الأنشطة
```javascript
// تسجيل كل عملية مهمة
securityManager.logActivity('action_name', { details });
```

---

## 💾 النسخ الاحتياطية

### إنشاء نسخة احتياطية تلقائية:
```javascript
// تُحفظ تلقائياً عند كل تغيير
dataBackup.createBackup(students);
```

### التصدير اليدوي:
1. اضغط "تصدير البيانات" في الأدوات المتقدمة
2. سيتم تحميل ملف JSON بالبيانات

### الاستيراد من ملف:
1. اضغط "استيراد البيانات"
2. اختر ملف JSON
3. وافق على استبدال البيانات

### سجل النسخ:
```javascript
// الحصول على سجل النسخ الاحتياطية
const history = dataBackup.getBackupHistory();
```

### التخزين:
- **المكان**: LocalStorage في المتصفح
- **السعة**: تصل إلى 5-10 MB حسب المتصفح
- **الفترة**: دائم حتى مسح الذاكرة المؤقتة

---

## 📱 الوضع بدون اتصال

### كيف يعمل:
1. **التثبيت الأول**: يحمل جميع الملفات الأساسية
2. **الاستخدام بدون اتصال**: يعمل من الذاكرة المؤقتة
3. **استعادة الاتصال**: يحدث البيانات تلقائياً

### المميزات:
- ✅ الوضع الكامل بدون إنترنت
- ✅ التزامن التلقائي
- ✅ لا توجد فقدان للبيانات

### تثبيت التطبيق على الهاتف:
**على Android (Chrome):**
1. افتح الموقع
2. اضغط القائمة (⋮)
3. اختر "تثبيت التطبيق"

**على iOS (Safari):**
1. افتح الموقع
2. اضغط مشاركة
3. اختر "أضف إلى الشاشة الرئيسية"

---

## 🛠️ أدوات المعلم

### اللوحة الأساسية:
- إضافة طلاب جدد
- عرض قائمة الطلاب
- إضافة مهارات
- تتبع التقدم

### الأدوات المتقدمة:

#### 📥 تصدير البيانات
```
النتيجة: ملف JSON يحتوي على جميع البيانات
الاستخدام: للنسخ الاحتياطية والنقل
```

#### 📤 استيراد البيانات
```
الملف: يجب أن يكون بصيغة JSON
التأكيد: يطلب تأكيد قبل الاستبدال
```

#### 📋 عرض السجلات
```
يعرض: آخر 20 نشاط
يتضمن: الوقت والعملية والتفاصيل
```

#### 🗑️ مسح البيانات
```
⚠️ تحذير: عملية حتمية ولا يمكن التراجع
يطلب: تأكيد مرتين
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "رقم الطالب غير صحيح"
**الحل:**
- تأكد من رقم الطالب (حروف وأرقام)
- تحقق من أنك تستخدم الرقم الصحيح
- اطلب من المعلم رقم جديد

### المشكلة: "لا يعمل بدون إنترنت"
**الحل:**
- تأكد من تحميل الموقع مرة واحدة بالإنترنت
- Service Worker يحتاج إلى تفعيل في المتصفح
- جرب متصفح مختلف

### المشكلة: "كلمة المرور خاطئة"
**الحل:**
- تأكد من كتابة: `admin` بصحة
- تحقق من Caps Lock
- انتظر 15 دقيقة بعد 5 محاولات فاشلة

### المشكلة: "البيانات لا تحفظ"
**الحل:**
- تأكد من تفعيل Cookies و LocalStorage
- جرب متصفح مختلف
- امسح الذاكرة المؤقتة جزئياً (ليس المواقع)

### المشكلة: "عدم ظهور الملفات"
**الحل:**
- تحقق من أن الرابط صحيح
- جرب رابط بديل
- تأكد من الاتصال بالإنترنت

---

## 📊 إحصائيات النظام

### متابعة الأداء:
```javascript
// عدد الطلاب
students.length

// إجمالي المهارات
students.reduce((sum, s) => sum + s.skills.length, 0)

// المهارات المنجزة
students.reduce((sum, s) => sum + s.skills.filter(sk => sk.done).length, 0)
```

### معدل الإنجاز:
```javascript
const total = students.reduce((sum, s) => sum + s.skills.length, 0);
const completed = students.reduce((sum, s) => sum + s.skills.filter(sk => sk.done).length, 0);
const percentage = total > 0 ? (completed / total * 100).toFixed(2) : 0;
console.log(`نسبة الإنجاز: ${percentage}%`);
```

---

## 🔑 مفاتيح اختصار مفيدة

| الاختصار | الوظيفة |
|---------|--------|
| `Ctrl + S` | حفظ البيانات (متصفح) |
| `F12` | أدوات المطور |
| `Ctrl + Shift + Delete` | مسح الذاكرة المؤقتة |
| `Ctrl + L` | تحديث الصفحة |

---

## 📞 تقارير الأخطاء والدعم

عند حدوث مشكلة:
1. افتح أدوات المطور (F12)
2. انسخ رسالة الخطأ من Console
3. تواصل مع دعم النظام

---

## 🎯 نصائح للاستخدام الأمثل

### للمعلم:
- ✅ احفظ نسخة احتياطية يومية
- ✅ تحقق من السجلات أسبوعياً
- ✅ أضف روابط مفيدة للملفات
- ✅ استخدم أسماء واضحة للمهارات

### للطالب:
- ✅ احتفظ برقم دخولك
- ✅ راجع الملفات بانتظام
- ✅ أخبر المعلم عن أي مشاكل
- ✅ استخدم الوضع بدون اتصال

---

**آخر تحديث**: 27/01/2026
**الإصدار**: 1.0.0
**الحالة**: ✅ منتج جاهز للاستخدام
